# Required — the AI planning:
#   export ANTHROPIC_API_KEY=sk-ant-...
ANTHROPIC_API_KEY=

# Optional — live place data from Geoapify (free, no credit card).
# Without it, Claude uses its own knowledge of the city.
#   export GEOAPIFY_API_KEY=your-key
GEOAPIFY_API_KEY=
